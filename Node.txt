GET - FAZ UMA REQUISIÇÃO E RECEBE
PUT - ALTERA UMA INFORMAÇÃO EXISTENTE NO SERVER 
POST - CRIA UMA NOVA INFORMAÇÃO(CONCLUSÃO DE CURSO);
DELETE - APAGA DADOS NO SERVER SIDE 

npm install readline-sync

FAZENDO UMA API

npm init para criar o package
npm install --save express (instalar para trabalhar com requisições REST
)
npm install --save body-parser(Permite a troca de objetos por requisições get e post)

npm install -g nodemon (para nao reiniciar toda hora o server no -g que é modo global ele instala no node para qualquer aplicação que eu for desenvolver)

nodemon server.js
